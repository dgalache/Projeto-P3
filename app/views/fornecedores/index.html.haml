%h1 Listing fornecedores

%table
  %thead
    %tr
      %th Nome
      %th
      %th
      %th

  %tbody
    - @fornecedores.each do |fornecedor|
      %tr
        %td= fornecedor.nome
        %td= link_to 'Show', fornecedor
        %td= link_to 'Edit', edit_fornecedor_path(fornecedor)
        %td= link_to 'Destroy', fornecedor, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Fornecedor', new_fornecedor_path
